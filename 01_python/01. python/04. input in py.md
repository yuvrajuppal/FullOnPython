In Python, when you want to take a number (like an integer) from the user, you use `input()` and then convert that string to `int()`.

```python
age = int(input("Enter your age: "))
print(f"You are {age} years old")
print(type(age))  # <class 'int'>
```

```python
age = int(input("Enter your age: "))  # If user types "abc"
# ❌ Error: ValueError: invalid literal for int()
```


