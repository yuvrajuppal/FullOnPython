

### **Question 1: The Synthetic Dataset Generator**

Imagine you are setting up a simulation for a store. You need to create three specific data structures to hold your initial information.

**Your Task:** Write a Python script using NumPy to create the following:

1. **Price Placeholders:** Create a 1D array of **10 zeros** representing the initial prices of 10 new products.
    
2. **The Identity Grid:** Create a **4x4 matrix** where every element is the number **1.0**. (Hint: Use `np.ones`).
    
3. **Coordinate Range:** Create a 1D array containing every **even number from 10 to 50** (inclusive of 10, but exclusive of 50).
    

---

### **Question 2: The Neural Network Initializer**

In Deep Learning, we often initialize "weights" (connections) with random numbers to help a model start learning. However, we need those results to be the same every time we test our code.

**Your Task:**

1. **Set the Seed:** Set the NumPy random seed to **42**.
    
2. **Standard Normal Weights:** Generate a **3x5 matrix** of random numbers drawn from a **Standard Normal distribution** (mean 0, variance 1).
    
3. **Random Age Data:** Generate a 1D array of **20 random integers** representing "ages." The ages should be between **18 and 65** (inclusive of 18, exclusive of 65).
    
4. **Verification:** Print all three outputs to the console.
    

---

### **Question 3: The "ROI" (Region of Interest) Extractor**

In image processing, images are just 2D or 3D arrays. Often, you only want to look at a specific part of an image (like a face or a license plate).

**Your Task:**

1. **Create the Image:** Create a **6x6 matrix** containing the numbers **1 to 36** (inclusive). Reshape it so it’s a nice 6x6 square.
    
2. **The Center Slice:** Use slicing to extract the **inner 2x2 square** from this matrix.
    
    - _Hint:_ This would be the elements at the intersection of rows 2-3 and columns 2-3 (using 0-indexing).
        
3. **The Masked Filter:** From your original 6x6 matrix, use **Boolean Indexing** to create a new 1D array that contains only the numbers that are **multiples of 5**.
    
4. **The Flip:** Print the **Transpose** of the original 6x6 matrix.
    

---

### **Question 4: Data Pipeline Assembly**

Data often arrives in "batches." You need to combine these batches and then prepare them for a specific mathematical model that requires a certain shape.

**Your Task:**

1. **The Batches:** Create two arrays:
    
    - `batch_1`: A 2x3 array of all **ones**.
        
    - `batch_2`: A 2x3 array of all **sevens** (use `np.full`).
        
2. **Merge:** Stack these two arrays **vertically** to create a single 4x3 matrix.
    
3. **The Auto-Reshape:** Take that 4x3 matrix and reshape it into a 2D array that has **exactly 2 columns**. Use the **-1** shortcut so NumPy calculates the number of rows for you.
    
4. **The Flatten:** Take your final result and convert it into a simple 1D array using the `.flatten()` method.
    

---


### **Question 5: The Sub-Matrix Surgeon**

In data science, we often need to "crop" our data to focus on a specific region or filter out noise.

**Your Task:**

1. **Create a 5x5 Grid:** Use `np.arange` and `reshape` to create a 5x5 matrix containing the numbers **0 to 24**.
    
2. **The "Internal" Slice:** Extract a **3x3 sub-matrix** from the center of this grid.
    
    - _Hint:_ This should include the values that aren't on the very top, bottom, left, or right edges.
        
3. **The Boolean Filter:** From the original 5x5 grid, extract all numbers that are **greater than 10 AND less than 20**.
    
4. **The Flip:** Print the **Transpose** of your 3x3 sub-matrix.
    

---

### **Question 6: Data Pipeline Technician**

Often, data comes in separate pieces (like different sensors or time intervals), and you need to stitch them together and reorganize them for a specific analysis.

**Your Task:**

1. **The Components:** Create two arrays:
    
    - `left_side`: A 2x2 array filled with the number **5**.
        
    - `right_side`: A 2x2 array filled with the number **10**.
        
2. **Horizontal Merger:** Stack these two side-by-side to create a **2x4 matrix**.
    
3. **The Shape-Shift:** Reshape that 2x4 matrix into a **4x2 matrix**.
    
4. **The Final Split:** Take your 4x2 matrix and use `np.vsplit` to break it into **two separate 2x2 arrays**. Print both resulting arrays to confirm they look correct.
    

---


