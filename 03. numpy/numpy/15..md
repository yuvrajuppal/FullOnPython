### **Basic Exploration & Filtering**

**1. The Headcount:** How many students are enrolled in the 'Computer Science' major?

- _Hint:_ Use basic boolean indexing or `.value_counts()`.
    

**2. The High Achievers:** Create a DataFrame containing only students who enrolled in or after 2021 and have a GPA strictly greater than 3.75. How many students fit this criteria?

- _Hint:_ Combine multiple conditions using `&`.
    

### **Numpy Math & Aggregation**

**3. The GPA Spread:** Using `numpy`, calculate the mean, median, and standard deviation of the GPAs across the entire student body.

- _Hint:_ Extract the column values and use `np.mean()`, `np.median()`, and `np.std()`.
    

**4. The 90th Percentile:** Find the 90th percentile GPA using `numpy`. Once you have that number, filter the DataFrame to show only the students who have a GPA equal to or above that percentile.

- _Hint:_ Look into `np.percentile()`.
    

### **Grouping & Sorting**

**5. Department Rankings:** Which major has the highest average GPA, and what is that GPA?

- _Hint:_ Use `.groupby()` and sort the results.
    

**6. Yearly Trends:** Group the dataset by `enrollment_year`. For each year, calculate the total number of students enrolled and their average GPA.

- _Hint:_ You can pass multiple aggregation functions using `.agg()`.
    

**7. Top of the Class:** Who are the top 5 students with the highest GPAs specifically within the 'Engineering' major?

- _Hint:_ Filter first, then use `.sort_values()` and `.head()`.
    

### **Data Manipulation & Logic**

**8. Academic Standing:** Use `np.where()` to create a new column called `academic_standing`.

- If a student's GPA is $\ge$ 3.5, label them 'Honors'.
    
- If the GPA is between 2.5 and 3.49, label them 'Good'.
    
- If the GPA is $<$ 2.5, label them 'Warning'.
    
- _Challenge:_ What are the total counts for each standing?
    

**9. Age Calculation:** Convert the `date_of_birth` column to datetime objects. Calculate the exact age of each student in years as of today. Who is the oldest student in the dataset?

- _Hint:_ Use `pd.to_datetime()` and subtract the birth dates from `pd.Timestamp.today()`.
    

**10. String Extraction:** Extract the domain name from the `email` column for every student and create a new column called `email_domain`. Since the mock data all uses the same domain, prove it by showing that there is only 1 unique value in this new column.

- _Hint:_ Look into Pandas `.str` accessors, specifically `.str.split()` or `.str.extract()`.